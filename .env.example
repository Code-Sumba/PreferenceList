# The Razorpay public key is fetched from the backend at payment time
# (POST /api/client/payments/create-order returns key_id) — nothing to
# configure here for that. This file is a placeholder for any future
# build-time config the client app needs.
